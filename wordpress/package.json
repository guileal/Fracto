{
  "name": "@fracto/wordpress-assets",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix packages/background-grid-black && npm run build --prefix packages/background-grid-light && npm run build --prefix packages/logo-01-black",
    "build:grid": "npm run build --prefix packages/background-grid-black",
    "build:grid-light": "npm run build --prefix packages/background-grid-light",
    "build:logo": "npm run build --prefix packages/logo-01-black"
  }
}
